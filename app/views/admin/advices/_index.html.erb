  <div class="da-panel collapsible">
    <div class="da-panel-header">
      <span class="da-panel-title">
        <img src="/assets/admin/images/icons/black/16/list.png" alt="" />
        Advice Listing
      </span>
    </div>
    <div class="da-panel-content">
      <table id="da-ex-datatable-default" class="da-table">
        <thead>
          <tr>
            <th>Title</th>
            <th>Image</th>
            <th>Description</th>
            <th>Hide / Show</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <% @advices.each do |advice| %>
            <tr>
              <td><%= advice.title.capitalize %></td>
              <td><%= image_tag advice.avatar.url(:small) %></td>
              <td><%= advice.description.html_safe %></td>
              <td>
                <% if(advice.visible == true) %>
                  <%= link_to "Hide", visible_admin_advice_path(:id => advice.id, :visible => false), method: :put %>
                <% else %>
                  <%= link_to "Show", visible_admin_advice_path(:id => advice.id, :visible => true), method: :put %>
                <% end %>  
              </td>
              <td class="da-icon-column">
                <%= link_to edit_admin_advice_path(advice.id) do %>  
                  <img src="/assets/admin/images/icons/color/pencil.png" />
                <% end %>

                <%= link_to admin_advice_path(advice.id), method: :delete, data: { confirm: 'Are you sure?' } do %>
                  <img src="/assets/admin/images/icons/color/cross.png" />
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <%= will_paginate  @advices %>  